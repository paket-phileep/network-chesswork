{
  "name": "network-chesswork",
  "packageManager": "yarn@4.3.1",
  "scripts": {
    "mkdir": "mkdir -p temp && touch temp/source-mac.json temp/dest-mac.json",
    "start": "yarn mkdir && sudo go run main.go"
  },
  "devDependencies": {
    "concurrently": "^7.0.0"
  }
}
